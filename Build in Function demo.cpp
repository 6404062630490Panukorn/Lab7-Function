#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int random1to100();

int main() {
    int N,i,j,rnum,numok;
    int A[100];
    scanf("%d",&N);
    if (N>100) 
        N=100;
    for (i=0;i<N;i++) {
        numok=1;
        while (numok==1)  {
            rnum=random1to100();
            //????????????????????????????????????????????????????? ?????????????????????
            for (j=0;j<i;j++){
				if (A[j]==rnum){
            		rnum = random1to100();
			   	}
        	}
        	numok = 0;
        }
        A[i]=rnum; //??????????????????????????????????????????????????????????
        
    }
    
    for (i=0;i<N;i++)
        printf("%d ",A[i]);
      
    return 0;
}

int random1to100() {
    int random_num = (rand() % 100) + 1;
    //printf("%d\n", random_num);
    return random_num;
}

